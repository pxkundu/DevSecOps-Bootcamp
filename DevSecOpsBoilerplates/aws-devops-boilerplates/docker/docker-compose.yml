version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: python-dockerfile
    ports:
      - "5000:5000"
    environment:
      - REDIS_HOST=redis
    depends_on:
      - redis
  redis:
    image: redis:6.2-alpine
    ports:
      - "6379:6379"
