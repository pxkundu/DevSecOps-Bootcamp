{
  "containerDefinitions": [
    {
      "name": "my-app",
      "image": "{{ECR_IMAGE}}",  # Replace with your ECR image URI
      "secrets": [
        {
          "name": "DB_PASSWORD",
          "valueFrom": "{{SECRETS_MANAGER_ARN}}"  # Replace with your Secrets Manager ARN
        }
      ]
    }
  ]
}
